//用指针的方法求字符串的长度
//int my_string(char arr[])
//{                           //定义两个指针
//	char* start = arr;//定义一个指针指向第一个元素的地址
//	char* end = arr;//定义一个指针指向最后一个元素的地址
//	while (*end != '\0')//当*end指针指向的不是最后一个元素的地址，因为字符串的结束标志是\0
//	{                   //所有当*end不等于0时end一直加加直到指向数组最后一个元素的地址
//		end++;//
//	}
//	return end - start;//指向数组最后一个元素的指针减去指向数组第一个元素的指针得到的是数组的大小
//}
//
//int main()
//{
//	char arr[] = { "bit" };
//	int len = my_string(arr);
//	printf("%d ", len);
//	return 0;
//}
//
