//另外一种算法
////喝汽水，1瓶汽水1元，2个空瓶可以换一瓶汽水，给20元，可以多少汽水(编程实现)。
int main()
{
	int empty = 0;//空瓶数
	int money = 0;//钱
	int total = 0;//计数器
	scanf_s("%d", &money);
	if (money == 0)
	{
		total = 0;
	}
	else
	{
		total = 2 * money - 1;
	}

	printf("%d ", total);
	return 0;
}
