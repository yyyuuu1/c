void swap(int* a, int* b)
{
	int tmp = 0;
	tmp = *a;
	*a = *b;
	*b = tmp;
}
int main()
{
	 int a = 10;
	 int b = 20;
	 printf("交换前%d %d\n", a, b);
	swap(&a,&b);
	printf("交换后%d %d\n", a, b);
	return 0;
}
