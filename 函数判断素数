void add(int a)
{
	int i = 0;
	for (i = 2; i < a ; i++)
	{
		if (a % i == 0)
		{
			break;
		}
	}
	if (a == i)
	{
		printf("是素数%d ", a);
	}
	else
		printf("不是素数\n");

}

int main()
{
	int a = 0;
	scanf("%d", &a);
	add(a);
	int i = 0;
	int j = 0;
	return 0;
}
